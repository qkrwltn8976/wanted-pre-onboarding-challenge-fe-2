/**
 * ID를 제외한 모든 속성을 수정할 수 있다.
 * 특정 할 일의 특정 태그를 수정할 수 있다.
 * @param {number} todoId
 * @param {string} content
 * @param {boolean} isCompleted
 * @param {number} categoryId
 * @param {number[]} tagIds
 * @returns {Todo}
 */
const updateTodo = (todoId, content, isCompleted, categoryId, tagIds) => {};
