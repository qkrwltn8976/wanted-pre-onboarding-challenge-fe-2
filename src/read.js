/**
 * 모든 할 일을 조회할 수 있다.
 *  @returns {Todo[]}
 */
const getAllTodos = () => {};

/**
 * ID를 기반으로 특정 할 일을 조회할 수 있다.
 * @param {number} todoId
 * @returns {Todo}
 */
const getTodoById = (todoId) => {};
